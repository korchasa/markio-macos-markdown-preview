# Images

An image on its own line is drawn:

![A generated gradient with a checkerboard](sample.png)

An image mentioned ![inline](sample.png) in a sentence keeps its marker and alt
text, because a picture in the middle of a line reads worse than a word.

A missing file falls back to the alt text:

![this file does not exist](nowhere.png)
